<div class="flex flex-col items-center my-8 max-w-4xl mx-auto w-full px-4">
  <h1 class="text-3xl font-medium my-3 md:my-8">{{ page.title }}</h1>
  <p class="max-w-6xl mx-auto px-4 prose text-justify">{{ page.content }}</p>
</div>